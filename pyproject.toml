[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "giotto-tda-cycles"
version = "0.1.0"
description = "Representative H1 cycle visualisation add-on for giotto-tda / gph-ripser"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
keywords = [
    "topological data analysis",
    "persistent homology",
    "representative cycles",
    "giotto-tda",
    "Vietoris-Rips",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
    "gph-ripser>=0.2.1",
    "giotto-tda>=0.6.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "scikit-learn>=1.3",
    "matplotlib>=3.7",
    "plotly>=5.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/sandythomas1/giotto-tda-cycles"
Issues   = "https://github.com/sandythomas1/giotto-tda-cycles/issues"

[tool.pytest.ini_options]
testpaths = ["tests"]
